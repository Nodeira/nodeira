# Override the WebSocket sync URL if your server is on a different host/path.
# Defaults to ws(s)://<current-host>/sync — works in dev (Vite proxy) and production.
# VITE_SYNC_WS_URL=wss://notes.example.com/sync
