<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only override: a development server runs on plain http, usually on the host machine.
  Release builds use src/main's copy, which forbids cleartext entirely.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
