node_modules/
dist/
.turbo/
*.env
.env.*
!.env.example
# Committed build config (non-secret): forces Metro's server root for release
# bundling in the pnpm monorepo. See the file for details.
!apps/mobile/.env

# Android signing keys — NEVER commit these.
*.jks
*.keystore
*.p12
*.p8
*.key
.tanstack
*.tsbuildinfo
*.timestamp-*.mjs
apps/api/uploads

ebooker/

# Design notes
design_notes/

# Claude Code local settings (personal, not project)
.claude/settings.local.json

# Turborepo prune output
out/

# Electron Forge / Vite build output
apps/desktop/.vite/

# Playwright e2e
apps/e2e/playwright-report/
apps/e2e/test-results/
apps/e2e/.seed-manifest.json

# Expo cruft

.expo/